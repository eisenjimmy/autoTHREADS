
## 0.2.18 release + Windows

- GitHub release includes macOS + Windows installers.
- Jarvis ubatch raised for mmproj; AutoThreads health-checks local LLM.

## Multi-part thread 1/n duplicate fix

- Persist `threadRootId` / `threadPartsPosted` after each part and **verify** save before continuing.
- On part-2+ failure, re-persist progress and include `root=` in the error for resume.
- `failDraft` never clears resume fields.
- Autopilot **resumes partial threads first** and will not plan a new post while one is incomplete.
- Scheduler retries partial threads after ~25s (not full 60s).
